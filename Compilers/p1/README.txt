using option 3

the table is named 'table.jpg'

the driver is called 'scanner'.  after making the executible, that's how the program is called.  in Main, testScanner is called.  it calls scanner and filter 2 repeatedly until the EOF token is returned.
The scanner function calls filter1 repeatedly until a token is built.  Filter1 grabs a single character at a time from the input file
